<!-- Autogenerated by mlir-tblgen; don't manually edit -->
# 'py_interp' Dialect

Dialect for interacting with an embedded Python interpreter


## Operations

### `py_interp.cast_from_pyobject` (::lingodb::compiler::dialect::py_interp::CastFromPyObject)

Syntax:

```
operation ::= `py_interp.cast_from_pyobject` $from $pythonType `->` type($to) attr-dict
```


#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>pythonType</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `from` | Type representing a Python object

#### Results:

| Result | Description |
| :----: | ----------- |
| `to` | any type


### `py_interp.cast_to_pyobject` (::lingodb::compiler::dialect::py_interp::CastToPyObject)

Syntax:

```
operation ::= `py_interp.cast_to_pyobject` $from `:` type($from) $pythonType attr-dict
```


#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>pythonType</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `from` | any type

#### Results:

| Result | Description |
| :----: | ----------- |
| `to` | Type representing a Python object


### `py_interp.create_module` (::lingodb::compiler::dialect::py_interp::CreateModule)

Syntax:

```
operation ::= `py_interp.create_module` $name `,` $code attr-dict
```


#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>name</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
<tr><td><code>code</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
</table>

#### Results:

| Result | Description |
| :----: | ----------- |
| `module` | Type representing a Python object


### `py_interp.dec_ref` (::lingodb::compiler::dialect::py_interp::DecRef)

Syntax:

```
operation ::= `py_interp.dec_ref` $obj attr-dict
```


#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `obj` | Type representing a Python object


### `py_interp.get_attr` (::lingodb::compiler::dialect::py_interp::GetAttr)

Syntax:

```
operation ::= `py_interp.get_attr` $on `->` $name attr-dict
```


#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>name</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `on` | Type representing a Python object

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | Type representing a Python object


### `py_interp.import` (::lingodb::compiler::dialect::py_interp::ImportOp)

Syntax:

```
operation ::= `py_interp.import` $name attr-dict
```


#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>name</code></td><td>::mlir::StringAttr</td><td>string attribute</td></tr>
</table>

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | Type representing a Python object


### `py_interp.inc_ref` (::lingodb::compiler::dialect::py_interp::IncRef)

Syntax:

```
operation ::= `py_interp.inc_ref` $obj attr-dict
```


#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `obj` | Type representing a Python object


### `py_interp.py_call` (::lingodb::compiler::dialect::py_interp::Call)

Syntax:

```
operation ::= `py_interp.py_call` $on `(` $args `)` $kwnames attr-dict
```

TODO: currently we do a hacky thing: args and kwargs are mixed...

#### Attributes:

<table>
<tr><th>Attribute</th><th>MLIR Type</th><th>Description</th></tr>
<tr><td><code>kwnames</code></td><td>::mlir::ArrayAttr</td><td>array attribute</td></tr>
</table>

#### Operands:

| Operand | Description |
| :-----: | ----------- |
| `on` | Type representing a Python object
| `args` | variadic of Type representing a Python object

#### Results:

| Result | Description |
| :----: | ----------- |
| `result` | Type representing a Python object


## Types

### PyObjectType

Type representing a Python object

Syntax: `!py_interp.py_object`



